# Сборка — генерируется из site/pages + site/src командой `node build.js`
# и выкладывается на хостинг через rsync (см. README). В репозитории
# не хранится.
site/dist/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Редакторы
.vscode/
.idea/
*.swp

# Node (на случай, если появятся зависимости)
node_modules/
npm-debug.log*

# Локальные конфиги инструментов
.claude/
