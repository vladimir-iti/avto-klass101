# Проект: новый сайт автошколы «Авто-Класс»

Домен: `avto-klass101.ru` (сохраняется). Название: «Авто-Класс» (сохраняется).
Конечная среда размещения — Beget, отдельный аккаунт под проект. Сейчас к Beget не подключаемся.

**Текущий этап:** готова первая полная версия сайта — 7 страниц (5 маркетинговых + документы
+ политика конфиденциальности), дизайн-система, новые тексты, адаптив, формы. Сайт собран
локально в `site/dist/` и не публиковался никуда за пределы `~/Projects/avto-klass/`.

## Главный документ

[`PROJECT-CONTENT.md`](PROJECT-CONTENT.md) — вся фактическая информация: организация, лицензии,
филиалы, площадка, кадры, автопарк, программы и часы, условия приёма, цены и рассрочка, экзамены,
медкомиссия, матбаза, преимущества, контакты, список сомнительных данных, разбор материалов
и распределение фактов и визуала по пяти посадочным страницам.

## Структура

```
avto-klass/
├── PROJECT-CONTENT.md
├── README.md
└── source-materials/
    ├── client/                    # материалы заказчика
    │   ├── _original-archives/    # исходные zip — не изменять
    │   ├── 01-logo/               # логотип: 2 векторных PDF + растр
    │   ├── 02-instructors/        # 8 портретов
    │   ├── 03-tractor-special-tech/
    │   ├── 04-classroom/
    │   ├── 05-video/
    │   └── _duplicates/           # точные дубли из архива
    └── old-site/                  # скачано с avto-klass101.ru
        ├── 01-avtopark-B/
        ├── 02-avtopark-moto/
        ├── 03-avtopark-C-D-pricepy/
        ├── 04-klassy-i-ofis/
        ├── 05-dokumenty-skany/
        ├── 06-dokumenty-pdf/      # 25 документов с Google Drive
        ├── 07-karty/
        ├── 08-partnery/
        ├── 09-stok-ne-nashe/      # сток со старого сайта, не использовать
        ├── 10-ui-ikonki/
        └── _pages-html/           # HTML старого сайта как источник фактов
```

## Правила работы

- Вся работа ведётся только внутри `~/Projects/avto-klass/`.
- Оригиналы материалов заказчика (zip-архивы) не изменяются.
- Материалы заказчика и материалы старого сайта хранятся раздельно — происхождение всегда видно.
- Старый сайт `avto-klass101.ru` используется только как источник фактов и медиафайлов;
  его тексты, структура и дизайн на новый сайт не переносятся.
- Утверждённые посадочные: `/`, `/category-b/`, `/category-a/`,
  `/professional/` (якоря `#c`, `#d`, `#be`, `#ce`, `#de`), `/tractor/`.
- Юридические и обязательные страницы образовательной организации — отдельно,
  в пять маркетинговых страниц не входят.

На рабочем столе есть символическая ссылка **«Авто-Класс»** на эту папку.

## Сайт (`site/`)

Статический сайт: HTML/CSS/vanilla JS, без фреймворков и CMS. Node используется только
как локальный сборщик шаблонов (`build.js`) — результат сборки в `site/dist/` полностью
статический и не зависит от Node на хостинге.

```bash
cd site
node build.js          # собрать dist/ из pages/ + partials/ + src/
node scripts/check.js  # проверка ссылок, якорей, meta, запрещённых данных
cd dist && python3 -m http.server 8123   # локальный просмотр
```

Подробности архитектуры, тексты, тестирование — в отчёте, переданном вместе с этой версией.
